<UserControl x:Class="Keyboard_simulator.controls.KeyboardButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Keyboard_simulator.controls"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="60">
    <Grid>
        <Border BorderBrush="#ab000000"  BorderThickness="0" x:Name="border" Background="#66000000" CornerRadius="10" Margin="1">
            <Grid x:Name="grid" SizeChanged="grid_SizeChanged">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition Width="20"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Label Grid.ColumnSpan="2" x:Name="LabelBase" Content="B" Padding="0" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="White">
                </Label>
                <Label Grid.Column="1"  x:Name="LabelSecond" Content="S" Padding="0" VerticalAlignment="Top" HorizontalAlignment="Center" Foreground="#666666"></Label>
            </Grid>
        </Border>
    </Grid>
</UserControl>
